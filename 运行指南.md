# 代码审核 Agent · 本地运行手册（WSL2 + Docker 版）

本手册把赛事方教程的步骤 1~9，改编成**你自己的「多 Agent 代码审查系统」**在本机 WSL2 + Docker Desktop 里跑通的全过程。
配套文件：

| 文件 | 作用 |
|------|------|
| `at/create_agents_messages.md` | 发给 Manager 的 4 条 Worker 创建消息 + 1 条 Team 创建消息 + 2 个代码审查任务 |
| `tools/mock_tool_server.py` | 模拟代码服务（自带 buggy_find_max 14 测试、pr_security_issue SQL 注入两个场景），让 Agent 有代码可审、有测试可跑 |

> 阅读约定：**「🔑 你要做」** = 必须在你自己的电脑/浏览器里操作（涉及账号/密码/命令）；**「📋 复制」** = 复制本手册里的命令/文字去粘贴。
> **🚨 黄金纪律**：下面所有安装/启动命令，都要在 **WSL2 Ubuntu 终端**里跑，**千万别用 Windows 的 Git Bash / PowerShell / CMD**（原因见步骤三）。

---

## 🔑 第 0 步：哪些事必须你亲自做

- 本机装好 **Docker Desktop（WSL2 后端）** 且 **WSL2 里装了 Ubuntu 发行版**、并开启 Docker 对 Ubuntu 的集成（下面步骤一会查）。
- 注册 DeepSeek 开放平台、充值、创建 API Key（调用大模型的"密码"）。
- 在 WSL2 Ubuntu 终端里跑**安装命令**；在 **Windows 的 Git Bash / PowerShell** 里**起模拟服务**（mock 不需要 docker.sock，不受"必须用 WSL2"限制，且这样容器才能用 `host.docker.internal` 连上它）；在 Element Web 里登录、粘贴消息、看结果。
- **我不会、也不能替你做上面这些**——它们都在你自己的电脑上跑（沙盒访问不到你的主机）。我能做的是：把 4 个 Agent 的设计、创建消息、可运行的服务、提交材料全部写好，你照着点就行。

---

## 🔑 步骤一：确认本机 WSL2 + Docker 就绪

1. Windows 开始菜单打开 **Docker Desktop**，确认左下角鲸鱼图标是 running（没跑就先启动它）。
2. 检查 WSL2 里有没有 Ubuntu：在 Windows 终端（PowerShell）跑 `wsl -l -v`，应能看到 `Ubuntu  Running`（或至少 installed）。没有就 `wsl --install -d Ubuntu` 装一个。
3. 开启 Docker 对 Ubuntu 的集成：Docker Desktop → Settings → Resources → WSL INTEGRATION → 打开 "Ubuntu" 那一项 → Apply & Restart。
4. 打开 **Ubuntu 终端**（开始菜单搜 Ubuntu 打开；别用 Git Bash）：
   - 跑 `docker ps`，应返回空列表且**不报错**（说明 Docker 在 WSL2 里连上了，且有真正的 unix socket）。
   - 若报 `command not found` 或连不上，回到第 3 步确认集成已开、Docker Desktop 在跑。

> 关键背景：AgentTeams 安装脚本只认 **unix 套接字文件**（/var/run/docker.sock），WSL2 Ubuntu 经 Docker 集成正好提供它；而 Windows 原生 Git Bash 用的是 Windows 命名管道（named pipe），脚本检测不到 → 会一路拉完镜像、却在最后一步创建 Manager 容器时因缺 socket 挂载而崩。所以**全程用 WSL2 Ubuntu 终端**。

---

## 🔑 步骤二：获取 DeepSeek API Key

1. 打开 DeepSeek 开放平台（浏览器），登录（手机号/微信）。
2. 进控制台 → **API Keys**；余额不足先充 **5 元**（跑一次 demo 不到 1 元）。
3. 点「创建 API Key」，名字随便填。生成后**先别关页面**，下一步安装要粘贴。
4. ⚠️ API Key 相当于密码，**不要发到群、GitHub、公开文档**。

---

## 步骤三：在 WSL2 Ubuntu 里安装 AgentTeams（约 10 分钟）

1. 在 **Ubuntu 终端**里，进入本运行包所在的 Windows 磁盘位置（这样配置文件留在你熟悉的 Windows 盘，方便我以后帮你改）：

```bash
cd /mnt/c/Users/Blue/Desktop/work/Datawhale/新智基座参赛/本地运行包
```

2. 📋 下载并运行官方安装脚本（在 Ubuntu 终端里执行）：

```bash
curl -fsSL https://raw.githubusercontent.com/agentscope-ai/AgentTeams/main/install/agentteams-install.sh -o agentteams-install.sh
bash agentteams-install.sh
```

> 若 raw 源连不上（国内网络偶尔 reset），改用镜像源下载：
> `curl -fsSL https://cdn.jsdelivr.net/gh/agentscope-ai/AgentTeams@main/install/agentteams-install.sh -o agentteams-install.sh`
> 然后同样 `bash agentteams-install.sh`。
> 想让工作空间落在 Windows 盘，可在跑之前加：`export AGENTTEAMS_WORKSPACE_DIR=/mnt/c/Users/Blue/agentteams-manager`

3. 安装选项按下面填（**优先看选项名称，别死记序号**）：

| 安装器提问 | 填什么 |
|------------|--------|
| 语言 | 默认中文 |
| 安装模式 | 默认「快速开始」 |
| 版本 | 默认 latest |
| LLM 供应商 | **OpenAI 兼容 API** |
| Base URL | `https://api.deepseek.com/v1` |
| 默认模型 ID | `deepseek-v4-flash` |
| 最大上下文长度 | `1000000` |
| 最大输出长度 | `384000` |
| 是否支持推理/思考 | `Y` |
| 是否支持图片输入 | `N` |
| **LLM API Key** | 📋 粘贴刚才的 DeepSeek Key |
| 记忆搜索配置 | 选 `3`（不启用） |
| Manager 运行时 | **QwenPaw（CoPaw）** |
| Worker 运行时 | **QwenPaw（CoPaw）** |
| 其他未说明的 | 直接回车用默认 |

4. 会下载镜像（没有进度条，耐心等几分钟）。完成后终端显示：AgentTeams 已启动、Element 地址、管理员用户名、自动生成的密码。
5. **忘记密码**：在 Ubuntu 终端执行 `grep '^AGENTTEAMS_ADMIN_PASSWORD=' /home/blue/agentteams-manager.env`，复制等号右边。

---

## 步骤四：打开 Element 聊天界面

1. 在你 **Windows 本机**的浏览器打开 `http://127.0.0.1:18088`（Docker 已把容器端口转发到本机，WSL2 里也能用同样地址）。
2. 点 **Sign in**；若要求填 Homeserver：点 Edit → 填 `http://127.0.0.1:18080` → Continue。
3. 登录：Username `blue`，Password 用安装时显示的密码（安装输出里那串；忘了看下方故障排查）。
4. 测试：打开 `Manager: default` 房间发句话，能对话即安装成功。

---

## 步骤五：启动模拟代码服务

> ⚠️ **在哪里跑**：在 **Windows 本机的 Git Bash（或 PowerShell）**里跑，**不要**在 WSL2 Ubuntu 里跑。
> 原因：AgentTeams 容器里写死的地址是 `http://host.docker.internal:18089`，而 `host.docker.internal` 在 Docker Desktop 里指向的是 **Windows 主机**。mock 服务只要跑在 Windows 主机上，容器就能连上它；如果跑在 WSL2 Ubuntu 发行版里，容器反而连不到（两者不在同一个网络地址上）。mock 只是个 Python 服务、不需要 docker.sock，所以用它跑完全没问题。

1. 在 **Windows 的 Git Bash** 里（保持这个窗口开着，demo 期间不要关）：

```bash
cd /mnt/c/Users/Blue/Desktop/work/Datawhale/新智基座参赛/本地运行包
python3 tools/mock_tool_server.py
```

看到 `mock_tool_server 已启动` 即成功。可选自检（另开一个 Git Bash 窗口）：

```bash
curl http://127.0.0.1:18089/health
# 应返回 {"ok": true, "service": "coding-review-mock-tool-gateway"}
```

> 若容器报连不上 mock（多为 Windows 防火墙拦了 18089 端口）：① 允许 Python 通过防火墙，或 ② 退一步——把 mock 跑在 WSL2 Ubuntu 里，并在任务消息里把 `host.docker.internal` 改成 Ubuntu 的 IP（Ubuntu 终端 `ip addr` 查 `eth0` 的 inet，如 `172.x.x.x`）。

---

## 步骤六：发创建消息给 Manager（建 4 个 Agent + 1 个 Team）

1. 打开 `at/create_agents_messages.md`（就在本运行包里），按 Ctrl+F 可搜索。
2. **分别**复制「第一部分」的 4 段消息，依次发给 Element Web 里的 `Manager: default` 房间（每条发完等 Manager 回"已创建"再发下一条）：
   - 1.1 创建 orchestrator（团队 lead）
   - 1.2 创建 reviewer
   - 1.3 创建 fixer
   - 1.4 创建 tester
3. 4 个 Worker 都创建成功、界面能看到条目后，复制「第二部分」创建 Team 的消息发给 Manager。
4. Manager 会自动完成：建 orchestrator → 确认 → 建 reviewer → 建 fixer → 建 tester → 最后建 coding-review Team。

> 不建议把 4 个 Worker + Team 塞进一条超长消息——Manager 是 LLM，太长容易漏建或建错。一步步来最稳。

---

## 步骤七：发两个代码审查任务给 Team 房间

1. 会话列表里找到以 `Team` 开头、对应 `coding-review` 的 Team 房间。
2. 复制「第三部分」**任务一**，在输入框先 `@orchestrator` 再粘贴发送。**等 CR-001 报告完整输出后**，再发**任务二**（一次一条，避免并发干扰）。
3. 你会看到：orchestrator 主动汇报各阶段（G1~G4、G5）→ reviewer 审查 → fixer 修复 → tester 实际跑测试（POST 到 mock 服务，返回 14/14 通过）→ orchestrator 汇总报告。

---

## 步骤八：提交初赛作品

初赛只需：**500 字简介 + 方案 PPT**（不强制可运行代码，跑通 demo 是为了理解多 Agent 协作）。
提交材料在本地仓库的 `新智基座参赛/初赛提交资料/` 文件夹（项目简介.md、项目初赛PPT.pptx、README.md、方案补充说明.md）。
把 PPT 压缩成 zip，打开赛事官网 https://www.goaihz.com/tracks?track=infra&channel=dw6 填写信息并提交（初赛最多提交 3 次，以最后一次为准）。

---

## 故障排查

- Manager 房间仍可用 → 直接把报错发给它，通常能自动解决大部分问题。
- 仍不行 → 保留报错截图，发学习群找助教。
- 忘密码 → Ubuntu 终端 `grep '^AGENTTEAMS_ADMIN_PASSWORD=' /home/blue/agentteams-manager.env`。
- **安装最后一步报 `invalid spec: :/var/run/docker.sock: empty section`** → 你八成是在 Windows Git Bash / PowerShell 里跑的安装脚本（缺 unix socket）。删掉半残状态（`agentteams` 容器/网络/卷 + `~/agentteams-manager.env` 和工作空间），改到 **WSL2 Ubuntu 终端**重跑。
- mock 服务连不上 → 检查 18089 终端是否还开着；确认任务消息里的地址（`host.docker.internal:18089`）与你的环境一致。
